# URL de l'API backend. En dev, laisser vide : le proxy Vite redirige /api vers :8000.
# En production (Vercel), renseigner l'URL Render, ex :
# VITE_API_URL=https://attestations-api.onrender.com/api
VITE_API_URL=
